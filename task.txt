Используя любой Template engine (Mustache.js, underscore.js метод template или другой)

Получить ajax^ом список юзеров (см. ниже примечания).
Вывести этих юзеров в таблице.
Если ни однин юзер не будет найден -- вывести соответствующее сообщение (несмотря на то, что какие-то юзеры будут в любом случае, все равно написать соответствующий код)
Написать функционал по удалению и созданию юзеров.

Написать калькулятор валют:
Данные о курсе валют брать из какого-либо сайта либо написать вручную (см. ниже примечания).
Список с какой валюты на какую конвертировать -- фиксированный (USD, EUR, RUB)




Примечания:

1)
Визуализацию делать на основе предоставленного шаблона (файл index.html)

2)
для получения списка юзеров в виде JSON можно использовать
либо
какой-либо файл, лежащий в директории проекта, например, /JSON/users.json
либо
данные для тестов, например, из сайта https://jsonplaceholder.typicode.com
а именно:
https://jsonplaceholder.typicode.com/users

3)
http://api.fixer.io/latest
по умолчанию -- получаешь JSON курса валют на основании евро
если нужна другая валюта -- нужно использовать GET параметр base
например, для доллара:
http://api.fixer.io/latest?base=usd

4) для стилизации можно использовать любой css framework, например, bootstrap.css

5) можно использовать библиотеки по типу jQuery

6) ajax запросы будут работать при имитации хостинга. Самый простой способ:
скачать http://brackets.io -- в нем есть встроенный хостинг
http://prntscr.com/gevnc1



Усложнение задачи
!!! делать только, если уверен, что успеешь !!!
!!! если не уверен -- лучше сделать задачу выше с помощью простого шаблонизатора !!!


* Реализовать данное задание, используя react.js с синтаксисом .jsx (для этого нужно пользоваться сборщиком)

В качестве сборщика рекомендую использовать https://facebook.github.io/react/docs/installation.html --> вкладка Create a New App -- сборщик webpack -- нам все настройки вшиты и разбираться в них не нужно будет

Потребуется локально установить у себя node.js

Если используешь сборщик, то brackets.io или другой текстовый редактор со встроенным хостингом не обязателен

В качестве инструмента для отправки запросов можно использовать https://www.npmjs.com/package/axios


** Реализвать данное задание, используя react.js + redux

!!!
Если знаия redux лишь очень поверхностные (т.е. без прохождения какого-либо tutorial или без чтения основ (http://redux.js.org/docs/basics/) -- лучше не браться
!!!